# Runtime marker — set once after applying the macOS launcher icon
.costvision-icon-set

# Claude Code internal working directories
.claude/

# Dependencies
node_modules/

# SQLite database (runtime data, not source)
calculator/data/

# Python bytecode — the deck builders import pptx_fixup, so running one
# leaves a __pycache__ behind at the repo root
__pycache__/
*.pyc
